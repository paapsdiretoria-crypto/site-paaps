# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Arquivos brutos / assets de produção (não vão para o repo)
insumos-compartilhados/fotos/
insumos-compartilhados/identidade-visual/
insumos-compartilhados/aplicacao-id-visual/
insumos-compartilhados/nossa-camiseta-paaps/
insumos-compartilhados/nossos-clientes-e-parceiros/
insumos-compartilhados/docs/*-imagens/

# Editor
.vscode/
*.swp
*.swo

# Temporários
*.tmp
*.log

# Configurações com credenciais (nunca commitar)
codigo/dashboard/js/config.js

# Chaves reais das automações (só o .env.example vai para o repo)
.env
**/.env

# Configurações locais do Claude Code (podem conter credenciais ou paths pessoais)
.claude/settings.local.json

# HyperFrames: repositório clonado (reprodutível via git clone) e skills instaladas (via npx skills add)
/hyperframes/
/.agents/
.claude/.ultimo-status-logado
/projetos/

# Instaladores e binários grandes baixados localmente (Ollama, Hermes Agent, apps .dmg/.app)
/Modelos/
*.dmg
*.app/
.playwright-mcp/

# Constelação: deps e chave da ponte
codigo/dashboard-agentes/ponte/node_modules/
codigo/dashboard-agentes/ponte/.env

# Pesquisa de TCC: lista de endereços das unidades e e-mails montados.
# Regra de automacoes/CLAUDE.md: lista de órgãos não entra no repo. O molde da forma
# fica em unidades-bh.example.json.
automacoes/pesquisa-tcc-bh/unidades-bh.json
automacoes/pesquisa-tcc-bh/previa/
automacoes/pesquisa-tcc-bh/anuencia-suas.pdf

# Newsletters puxadas por IMAP (automacoes/conteudo-pipeline/newsletters/puxar-newsletters.mjs):
# carregam nome e e-mail real de remetente terceiro (dado pessoal). O Radar lê local,
# nunca precisa estar versionado.
conteudo/ciclos/*/B0-newsletters.md

# vídeo grande, fora do git desde 01/09/2026 (travava o push, acima do limite do GitHub)
codigo/site/pitch-serasa/img/final - cópia.mp4
