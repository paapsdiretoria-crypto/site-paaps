{
  "env": {
    "HYPERFRAMES_FFMPEG_PATH": "/Users/mac/bin/ffmpeg",
    "HYPERFRAMES_FFPROBE_PATH": "/Users/mac/bin/ffprobe"
  },
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\"; mkdir -p sessoes; D=$(date '+%Y-%m-%d'); T=$(date '+%H:%M'); F=\"sessoes/sessao-${D}.md\"; L=\".claude/.ultimo-status-logado\"; test -f \"$F\" || echo \"# Log de Sessão - ${D}\" > \"$F\"; C=$(git status --short 2>/dev/null | grep -v 'sessoes/'); if [ -n \"$C\" ] && [ \"$C\" != \"$(cat \"$L\" 2>/dev/null)\" ]; then echo \"\" >> \"$F\"; echo \"## ${T}\" >> \"$F\"; echo '```' >> \"$F\"; echo \"$C\" >> \"$F\"; echo '```' >> \"$F\"; echo \"\" >> \"$F\"; echo \"$C\" > \"$L\"; fi"
          },
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && if [ -n \"$(git status --porcelain 2>/dev/null)\" ]; then A=$(git status --porcelain | sed 's/^...//' | cut -d/ -f1 | sort -u | head -6 | paste -s -d ',' - | sed 's/,/, /g'); git add -A && git commit -m \"auto: ${A} - $(date '+%Y-%m-%d %H:%M')\" && git push; fi"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "codegraph prompt-hook"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git add*)",
      "Bash(git commit*)",
      "Bash(git push*)",
      "Bash(git log*)",
      "Bash(~/bin/gh*)",
      "mcp__codegraph__*",
      "WebSearch",
      "WebFetch",
      "Read(//Users/mac/Documents/SITE PAAPS/**)",
      "Write(//Users/mac/Documents/SITE PAAPS/**)",
      "Edit(//Users/mac/Documents/SITE PAAPS/**)",
      "mcp__notion__*",
      "mcp__claude_ai_Notion__*",
      "mcp__14be8da9-3f38-4015-8c7e-87b9ab437a0a__*",
      "mcp__n8n-mcp__n8n_executions",
      "mcp__n8n-mcp__n8n_list_workflows",
      "mcp__n8n-mcp__n8n_get_workflow"
    ]
  }
}
